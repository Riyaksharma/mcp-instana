# JWT Authentication Configuration Example for MCP Instana Server
# Copy this file to .env and update the values for your environment

# Enable JWT authentication
FASTMCP_AUTH_JWT_ENABLED=true

# JWT Audience (must match the 'aud' claim in your JWT tokens)
FASTMCP_AUTH_JWT_AUDIENCE=mcp-instana-api

# JWT Algorithm (RS256, ES256, HS256, HS384, HS512)
FASTMCP_AUTH_JWT_ALGORITHM=HS256

# Required scopes (comma-separated, optional)
FASTMCP_AUTH_JWT_REQUIRED_SCOPES=read:data,write:data

# Choose ONE of the following key configuration options:

# Option 1: Symmetric Key (HMAC) - Recommended for testing
FASTMCP_AUTH_JWT_PUBLIC_KEY=your-shared-secret-key-minimum-32-characters-long

# Option 2: JWKS Endpoint (Recommended for production)
# FASTMCP_AUTH_JWT_JWKS_URI=https://your-auth-server.com/.well-known/jwks.json

# Option 3: Static Public Key (RSA/ECDSA)
# FASTMCP_AUTH_JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----
# MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...
# -----END PUBLIC KEY-----

# Instana credentials (for non-JWT mode or fallback)
# INSTANA_API_TOKEN=your_instana_api_token_here
# INSTANA_BASE_URL=https://your-instana-instance.instana.io
